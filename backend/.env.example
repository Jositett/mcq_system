# Example environment variables for MCQ System Backend

# Secret key for JWT signing (change in production!)
JWT_SECRET=your-secret-key

# Database URL (SQLAlchemy format)
DATABASE_URL=sqlite:///./app.db

# CORS origins (comma-separated, e.g. http://localhost:3000,https://yourdomain.com)
CORS_ORIGINS=http://localhost:3000

# Set to 'production' in production
ENV=development

# (Optional) HTTPS settings
SSL_KEYFILE=path/to/privkey.pem
SSL_CERTFILE=path/to/fullchain.pem
